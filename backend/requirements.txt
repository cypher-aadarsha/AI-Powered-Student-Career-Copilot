# --- Web framework ---
fastapi==0.135.0  # bumped from 0.115.6 in Phase 11 to pull a starlette without PYSEC-2026-161/248/249/2280/2281
starlette==1.3.1  # pin explicitly (see above) rather than let fastapi's open-ended range silently drift
uvicorn[standard]==0.32.1
python-multipart==0.0.32  # bumped from 0.0.19 in Phase 11 (PYSEC-2026-1852/3038/3037/3036/3040/3039)

# --- Settings / validation ---
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Auth (wired in Phase 3) ---
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4's self-test breaks on bcrypt>=4.1 (__about__ removed, stricter 72-byte check)
python-jose[cryptography]==3.4.0  # bumped from 3.3.0 in Phase 11 (PYSEC-2024-232/233, PYSEC-2025-185)

# --- Resume parsing & AI (Phase 5) ---
pdfplumber==0.11.10  # bumped from 0.11.4 in Phase 11 — pulls a patched pdfminer.six (PYSEC-2026-1762/1761);
                     # this is the library that parses untrusted user-uploaded PDFs, so it matters more than most
python-docx==1.1.2
httpx==0.28.1  # also used at runtime by the 'llm' AI provider (app/ai/provider.py), not just tests

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
reportlab==4.4.0  # generates throwaway PDF bytes for test_resumes.py — never used at runtime
